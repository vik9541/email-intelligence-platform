# Quick Start: GitHub Secrets Setup

## ✅ Минимальная настройка для зелёных CI workflows

Настроим только **ОБЯЗАТЕЛЬНЫЕ** secrets для работы CI/CD.

---

## 🐳 DOCKER CREDENTIALS (обязательно)

### Шаг 1: Создай GitHub Personal Access Token

1. **Открой:** https://github.com/settings/tokens/new

2. **Заполни:**
   - **Note:** `Email Intelligence Platform GHCR`
   - **Expiration:** `90 days` (или `No expiration` для удобства)
   - **Select scopes:**
     - ✅ `write:packages`
     - ✅ `read:packages`
     - ✅ `delete:packages`

3. **Нажми:** `Generate token`

4. **ВАЖНО:** Скопируй токен СЕЙЧАС! Выглядит как `ghp_xxxxxxxxxxxxxxxxxxxx`
   - Показывается только один раз
   - Сохрани в надёжное место

### Шаг 2: Добавь secrets в GitHub

1. **Открой:** https://github.com/vik9541/email-intelligence-platform/settings/secrets/actions

2. **Нажми:** `New repository secret`

3. **Добавь первый secret:**
   ```
   Name: DOCKER_USERNAME
   Secret: vik9541
   ```
   Нажми `Add secret`

4. **Добавь второй secret:**
   ```
   Name: DOCKER_PASSWORD
   Secret: ghp_xxxxxxxxxxx  (твой PAT из шага 1)
   ```
   Нажми `Add secret`

✅ **Готово!** Теперь workflows смогут пушить Docker images в GHCR.

---

## ☸️ KUBECONFIG (опционально, для production)

**ПОКА НЕ НУЖЕН!** Добавишь когда будет Kubernetes кластер.

### Когда понадобится:
- При создании production кластера (GKE/EKS/AKS/Minikube)
- Перед деплоем в production (понедельник 09:00 UTC)

### Как добавить (когда будет кластер):

```bash
# На машине где есть kubectl доступ к кластеру
cat ~/.kube/config | base64 -w 0

# Результат (длинная строка) добавить как GitHub Secret:
# Name: KUBECONFIG
# Secret: <base64 encoded config>
```

### Временное решение:
- Deploy workflow будет падать - **это нормально**
- Или отключи его временно: добавь `if: false` в `.github/workflows/deploy.yml`

---

## 🚫 НЕ НУЖНЫ СЕЙЧАС

### SLACK_WEBHOOK_URL
- Используется только для alerts
- Можно настроить позже
- Без него система работает нормально

### AWS Credentials
- Используются только для S3 backups
- Можно хранить бекапы локально
- Или использовать другое хранилище

---

## ✅ Проверка

После настройки Docker credentials:

1. **Триггер CI:**
   ```bash
   git commit --allow-empty -m "ci: test docker credentials"
   git push origin main
   ```

2. **Проверь GitHub Actions:**
   https://github.com/vik9541/email-intelligence-platform/actions

3. **Ожидаемый результат:**
   - ✅ `test.yml` - зелёный (проходит тесты)
   - ✅ `lint.yml` - зелёный (проверяет код)
   - ❌ `deploy.yml` - красный (нет KUBECONFIG - нормально)
   - ❌ `security-scan.yml` - может быть красным (нет кластера - нормально)

---

## 🎯 Итого

**Минимум для работающего CI:**
- ✅ DOCKER_USERNAME
- ✅ DOCKER_PASSWORD

**Остальное - опционально до production.**
