# 📘 Как использовать GitHub Copilot для генерации кода

> **Для новичков в GitHub Copilot**  
> **Время чтения:** 10 минут  
> **Последнее обновление:** 15 декабря 2025 г.

---

## 🎯 ЧТО ЭТО?

Пошаговая инструкция как использовать GitHub Copilot для генерации кода Email Intelligence Platform.

**Вы научитесь:**
- ✅ Активировать GitHub Copilot в VS Code
- ✅ Открыть Copilot Chat
- ✅ Правильно формулировать команды
- ✅ Исправлять ошибки в сгенерированном коде
- ✅ Troubleshooting частых проблем

---

## 📋 ПРЕДВАРИТЕЛЬНЫЕ ТРЕБОВАНИЯ

### 1. Установка VS Code

```bash
# Windows
winget install Microsoft.VisualStudioCode

# macOS
brew install --cask visual-studio-code

# Linux (Ubuntu/Debian)
sudo snap install code --classic
```

**Проверка:**
```bash
code --version
# Ожидаемый результат: 1.95.0 или выше
```

---

### 2. Установка GitHub Copilot Extension

**Вариант A: Через VS Code UI**
1. Откройте VS Code
2. Нажмите `Ctrl+Shift+X` (или `Cmd+Shift+X` на macOS)
3. Найдите "GitHub Copilot"
4. Нажмите "Install"

**Вариант B: Через командную строку**
```bash
code --install-extension GitHub.copilot
code --install-extension GitHub.copilot-chat
```

**Проверка:**
```bash
code --list-extensions | grep -i copilot
# Ожидаемый результат:
# GitHub.copilot
# GitHub.copilot-chat
```

---

### 3. Активация GitHub Copilot

1. Откройте VS Code
2. Нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на macOS)
3. Введите "GitHub Copilot: Sign In"
4. Нажмите Enter
5. Авторизуйтесь через GitHub

**Проверка:**
- В правом нижнем углу VS Code должна быть зелёная галочка: `✓ Copilot`
- Если видите красный крестик `✗ Copilot` → перезапустите VS Code

---

## 🚀 БЫСТРЫЙ СТАРТ

### Шаг 1: Открыть Copilot Chat

**Вариант A: Через Command Palette**
1. Нажмите `Ctrl+Shift+P` (или `Cmd+Shift+P` на macOS)
2. Введите "GitHub Copilot: Open Copilot Chat"
3. Нажмите Enter

**Вариант B: Через иконку**
- Кликните иконку чата (💬) в левой панели VS Code

**Вариант C: Через горячую клавишу**
- Windows/Linux: `Ctrl+Alt+I`
- macOS: `Cmd+Opt+I`

**Результат:**
- Справа появится панель с чатом
- Вверху будет написано "GitHub Copilot Chat"

---

### Шаг 2: Скопировать команду из COPILOT-QUICK-START.md

1. Откройте файл `docs/copilot/COPILOT-QUICK-START.md`
2. Найдите "КОМАНДА 1: Создать PostgreSQL schema"
3. Скопируйте весь текст команды (начинается с "Создай файл...")

**Пример:**
```
Создай файл `migrations/001_init_schema.sql` с PostgreSQL schema для Email Intelligence Platform.

**Требования:**

1. **Таблица `emails`** (основная таблица писем):
   - `id` UUID PRIMARY KEY
   - `message_id` VARCHAR(255) UNIQUE (IMAP Message-ID)
   ...
```

---

### Шаг 3: Вставить команду в Copilot Chat

1. Кликните в поле ввода Copilot Chat (внизу панели)
2. Вставьте скопированную команду: `Ctrl+V` (или `Cmd+V` на macOS)
3. Нажмите Enter

**Ожидаемый результат:**
- Copilot начнёт думать (появится анимация загрузки)
- Через 10-30 секунд появится ответ с кодом

---

### Шаг 4: Применить сгенерированный код

**Вариант A: Автоматическое применение (рекомендуется)**

Copilot предложит кнопки:
- **"Apply in Editor"** - создать файл и вставить код
- **"Insert at Cursor"** - вставить код в текущий файл
- **"Copy"** - скопировать код в буфер обмена

Нажмите **"Apply in Editor"**.

**Вариант B: Ручное применение**

1. Скопируйте сгенерированный код
2. Создайте файл вручную:
   ```bash
   mkdir -p migrations
   touch migrations/001_init_schema.sql
   ```
3. Откройте файл в VS Code
4. Вставьте скопированный код

---

### Шаг 5: Проверить сгенерированный код

**Быстрая проверка:**
```bash
# Подсчитать строки кода
wc -l migrations/001_init_schema.sql
# Ожидаемый результат: ~350 строк

# Проверить синтаксис SQL (если установлен sqlfluff)
sqlfluff lint migrations/001_init_schema.sql
```

**Детальная проверка:**
1. Откройте файл в VS Code
2. Прочитайте первые 20 строк
3. Убедитесь, что есть:
   - `CREATE TABLE emails (...)`
   - `CREATE INDEX idx_emails_...`
   - `CREATE TRIGGER trg_emails_...`

---

### Шаг 6: Повторить для остальных команд

1. Вернитесь к файлу `COPILOT-QUICK-START.md`
2. Скопируйте КОМАНДУ 2 (Pydantic модели)
3. Вставьте в Copilot Chat
4. Примените код
5. Повторите для КОМАНД 3-8

---

## 💡 BEST PRACTICES

### ✅ Правильная формулировка команд

**Хорошо:**
```
Создай файл `app/services/email_parser.py` с асинхронным парсером email.

**Требования:**
1. Класс `EmailParserService` с методом `parse_email(raw_email: bytes) -> EmailModel`
2. Поддержка MIME multipart (text/plain, text/html, attachments)
3. Извлечение headers: From, To, CC, BCC, Subject, Date
4. Обработка вложений с сохранением в S3
5. Использовать библиотеку `email` из Python standard library

**Пример входных данных:**
```raw
From: sender@example.com
To: recipient@example.com
Subject: Test Email
Content-Type: text/plain

Hello World
```

**Ожидаемый результат:** Объект `EmailModel` с полями `from_address`, `to_addresses`, `subject`, `body_text`
```

**Плохо:**
```
Напиши парсер email
```

**Почему плохо:**
- Нет контекста (какой язык? какие библиотеки?)
- Нет требований (что именно парсить?)
- Нет примеров (как должен выглядеть результат?)

---

### ✅ Использование контекста

**Хорошо:**
```
Создай файл `app/main.py` с FastAPI приложением.

**Контекст:**
- Используй Pydantic модели из `app/models/email_models.py`
- Используй EmailParserService из `app/services/email_parser.py`
- Используй DatabaseService из `app/services/database.py`

**Endpoints:**
1. `POST /parse` - принимает raw email (bytes), возвращает EmailModel
2. `GET /health` - health check
3. `GET /ready` - readiness check (проверка БД)
```

**Плохо:**
```
Создай FastAPI приложение для парсинга email
```

**Почему плохо:**
- Copilot не знает, какие сервисы уже созданы
- Может сгенерировать дублирующий код

---

### ✅ Разбиение больших команд

**Если команда генерирует >500 строк кода:**

**Вариант A: Разбить на 2-3 команды**
```
# Команда 1A: Создать базовый класс
Создай файл `app/services/email_parser.py` с базовым классом `EmailParserService`.

**Методы:**
1. `__init__(self, s3_client: S3Client)`
2. `parse_headers(self, raw_email: bytes) -> Dict[str, str]`

# Команда 1B: Добавить парсинг тела письма
Допиши в файл `app/services/email_parser.py` метод `parse_body(self, raw_email: bytes) -> Tuple[str, str]`.

**Требования:**
1. Извлечь text/plain и text/html части
2. Обработать encoding (UTF-8, ISO-8859-1, Windows-1251)

# Команда 1C: Добавить обработку вложений
Допиши в файл `app/services/email_parser.py` метод `parse_attachments(self, raw_email: bytes) -> List[Attachment]`.
```

---

### ✅ Использование примеров

**Всегда добавляйте примеры входных/выходных данных:**

```
**Пример входных данных:**
```python
raw_email = b"""From: sender@example.com
To: recipient@example.com
Subject: Test
Content-Type: text/plain; charset=utf-8

Hello World
"""
```

**Ожидаемый результат:**
```python
EmailModel(
    from_address="sender@example.com",
    to_addresses=["recipient@example.com"],
    subject="Test",
    body_text="Hello World",
    body_html=None,
    attachments=[]
)
```
```

---

## 🛠️ TROUBLESHOOTING

### Проблема 1: Copilot не отвечает

**Симптомы:**
- После отправки команды ничего не происходит
- Или появляется ошибка "Copilot is not available"

**Решение:**

1. **Проверьте интернет соединение**
   ```bash
   ping github.com
   ```

2. **Проверьте статус Copilot**
   - Откройте https://www.githubstatus.com/
   - Убедитесь, что "Copilot" зелёный

3. **Перезапустите VS Code**
   ```bash
   # Закройте VS Code
   # Откройте снова
   code .
   ```

4. **Переавторизуйтесь**
   - `Ctrl+Shift+P` → "GitHub Copilot: Sign Out"
   - `Ctrl+Shift+P` → "GitHub Copilot: Sign In"

---

### Проблема 2: Copilot генерирует неправильный код

**Симптомы:**
- Код не компилируется
- Логика неверная
- Не соответствует требованиям

**Решение:**

1. **Добавьте больше контекста**
   ```
   Создай файл `app/services/email_parser.py`.
   
   **ВАЖНО:**
   - НЕ используй библиотеку `mailparser` (её нет в requirements.txt)
   - Используй только стандартную библиотеку `email`
   - Метод `parse_email` должен быть асинхронным (`async def`)
   ```

2. **Используйте команду `/fix`**
   ```
   /fix В файле `app/services/email_parser.py` метод `parse_email` должен быть async, но ты создал sync версию. Исправь.
   ```

3. **Попросите исправить конкретную ошибку**
   ```
   В файле `app/services/email_parser.py` строка 45:
   
   ```python
   attachment_data = part.get_payload(decode=True)
   ```
   
   Ошибка: `AttributeError: 'str' object has no attribute 'get_payload'`
   
   Исправь: нужно проверить тип `part` перед вызовом `get_payload`.
   ```

---

### Проблема 3: Copilot генерирует слишком мало кода

**Симптомы:**
- Ожидали 350 строк, получили 50
- Код неполный (нет обработки ошибок, тестов)

**Решение:**

1. **Попросите добавить недостающие части**
   ```
   Допиши в файл `app/services/email_parser.py`:
   
   1. Обработку исключений (try/except для UnicodeDecodeError, InvalidHeaderDefect)
   2. Логирование через `logging.getLogger(__name__)`
   3. Type hints для всех методов
   4. Docstrings в формате Google Style
   ```

2. **Уточните требования**
   ```
   Создай файл `app/services/email_parser.py` (~250 строк кода).
   
   **Требования:**
   - Класс EmailParserService
   - 5 методов: parse_email, parse_headers, parse_body, parse_attachments, upload_to_s3
   - Обработка ошибок для каждого метода
   - Unit тесты в отдельном файле tests/test_email_parser.py
   ```

---

### Проблема 4: Copilot генерирует код на другом языке

**Симптомы:**
- Просили Python, получили JavaScript
- Просили SQL, получили Python

**Решение:**

1. **Явно укажите язык в команде**
   ```
   Создай файл `migrations/001_init_schema.sql` с PostgreSQL schema.
   
   **ВАЖНО:** Используй ТОЛЬКО PostgreSQL SQL синтаксис (НЕ MySQL, НЕ SQLite).
   ```

2. **Укажите версию языка**
   ```
   Создай файл `app/main.py` с FastAPI приложением.
   
   **ВАЖНО:** Используй Python 3.11+ синтаксис (async/await, match/case, type hints).
   ```

---

### Проблема 5: Copilot создаёт файл не в той папке

**Симптомы:**
- Просили создать `migrations/001_init_schema.sql`
- Получили `001_init_schema.sql` в корне проекта

**Решение:**

1. **Используйте абсолютные пути**
   ```
   Создай файл по пути `C:\Projects\email-service\migrations\001_init_schema.sql`
   ```

2. **Или переместите файл вручную**
   ```bash
   mkdir -p migrations
   mv 001_init_schema.sql migrations/
   ```

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

### Официальная документация

- [GitHub Copilot Documentation](https://docs.github.com/en/copilot)
- [VS Code Copilot Extension](https://marketplace.visualstudio.com/items?itemName=GitHub.copilot)
- [Copilot Chat Best Practices](https://github.blog/2023-06-20-how-to-write-better-prompts-for-github-copilot/)

### Видеоуроки

- [GitHub Copilot Quickstart (5 min)](https://www.youtube.com/watch?v=Fi3AJZZregI)
- [Copilot Chat Tutorial (15 min)](https://www.youtube.com/watch?v=3surPGP7_4o)

### Альтернативы GitHub Copilot

Если GitHub Copilot недоступен, используйте:

1. **ChatGPT-4** (https://chat.openai.com/)
   - Скопируйте команды из `COPILOT-QUICK-START.md`
   - Вставьте в ChatGPT
   - Скопируйте сгенерированный код в VS Code

2. **Claude 3.5 Sonnet** (https://claude.ai/)
   - Аналогично ChatGPT

3. **Codeium** (бесплатная альтернатива Copilot)
   ```bash
   code --install-extension Codeium.codeium
   ```

---

## ✅ ЧЕКЛИСТ ДЛЯ НОВИЧКОВ

Перед началом работы убедитесь:

- [ ] VS Code установлен (`code --version` работает)
- [ ] GitHub Copilot extension установлен
- [ ] Copilot активирован (зелёная галочка в статус-баре)
- [ ] Copilot Chat открывается (`Ctrl+Alt+I`)
- [ ] Прочитан файл `README-START-HERE.md`
- [ ] Открыт файл `COPILOT-QUICK-START.md`

**Готово?** Переходите к выполнению КОМАНДЫ 1-8!

---

## 🎓 СЛЕДУЮЩИЕ ШАГИ

После того как разберётесь с Copilot:

1. **Откройте `COPILOT-QUICK-START.md`**
   - Выполните КОМАНДУ 1-8 по порядку

2. **Изучите сгенерированный код**
   - Прочитайте каждый файл
   - Добавьте свои комментарии

3. **Запустите тесты**
   ```bash
   pytest -v --cov=app
   ```

4. **Прочитайте полное ТЗ**
   - `TASK-EMAIL-ERP-COMPLETE.md` для глубокого понимания архитектуры

---

**Последнее обновление:** 15 декабря 2025 г.  
**Версия:** 1.0.0  
**Автор:** Email Intelligence Platform Team  
**Лицензия:** MIT
